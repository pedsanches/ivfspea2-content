# Thesis analyses

Place analysis code that is specific to the master's thesis here. Reuse shared
analysis modules from `src/python/analysis`, `src/python/fla`, and
`src/python/dynamics` instead of copying them.

Every new experiment or analysis must persist both IGD and HV at minimum.
