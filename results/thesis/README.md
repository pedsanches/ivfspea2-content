# Thesis results

This directory is the canonical destination for tables, figures, audit files,
and other derived outputs created specifically for the master's thesis.

Do not place raw experiment output here. Raw data belong under `data/raw/`, and
shared processed data belong under `data/processed/`.
