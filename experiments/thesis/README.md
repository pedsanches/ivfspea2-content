# Thesis experiment launchers

Place only thesis-specific MATLAB experiment entry points here. Reuse canonical
algorithm implementations and shared experiment helpers from the repository.

Every evaluated configuration/problem must persist both IGD and HV.
