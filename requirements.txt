# IVF-SPEA2 Python Dependencies — supported ranges for new work.
#
# To REPRODUCE committed artifacts byte for byte, install requirements.lock.txt
# instead. This file describes what the code supports; the lock file describes
# the one environment the published checksums were computed against.
#
# Requires Python >= 3.11 (thesis/masters/scripts/validate_data_sources.py uses
# the stdlib tomllib, added in 3.11). Verified working on 3.14.

pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
numpy>=1.26
scipy>=1.10,<2.0
scikit-learn>=1.2,<2.0
pymatreader>=0.0.12
tqdm>=4.60,<5.0
statsmodels>=0.14,<1.0

# Development / Testing
pytest>=7.0,<9.0
